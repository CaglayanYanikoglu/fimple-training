
const headerEl = document.querySelector('#header-id');
// document.getElementById('header-id');
// console.log(document.getElementsByClassName('header'));

/* const newHeader = document.getElementsByClassName('header');

newHeader[0].style.color = 'red';

headerEl.innerHTML = 'Test'; */

// headerEl.style.color = 'red';

// headerEl.classList.add('new-class');
// console.log(headerEl.classList);
// DOM manipülasyonu

/* const textEl = document.querySelector('.text');
textEl.style.color = 'green';
console.log(textEl); */
/* 
const textEls = document.querySelectorAll('.text');
textEls.forEach(element => {
  console.log(element);
  element.style.color = 'blue';
  // element.innerHTML = 'New HTml';
  // element.innerText = '<b>New HTml</b>';
  element.innerHTML = '<b>New HTml</b>';

})
console.log(textEls);
 */


// document.addEventListener('')


/* function btnClick() {
  console.log('it is clicked ');
} */

const btnEl = document.querySelector('#btn');
// console.log(btnEl);

const buttonClicked = () => {
  /* const fruitsContainer = document.querySelector('.fruits');
  console.log(fruitsContainer);
  const newLiElement = document.createElement('li');
  newLiElement.innerText = 'Armut';
  newLiElement.style.backgroundColor = 'blue';

  fruitsContainer.appendChild(newLiElement); */

/*   const fruits = ['apple', 'mango', 'pineapple'];

  const fruitsContainer = document.querySelector('.fruits-2');
  let text = '';
  fruits.forEach(item => {
    text += `<li>${item}</li>`;
  });
  fruitsContainer.innerHTML = text; */

}

// btnEl.addEventListener('click', buttonClicked);
